# ⚛️ DhakaCart Frontend (UI)

**এটি কি? (What is this?)**
এটি DhakaCart-এর ক্লায়েন্ট সাইড অ্যাপ্লিকেশন যা ইউজার ব্রাউজারে দেখে। এটি **React.js (v18)** দিয়ে তৈরি এবং প্রোডাকশনে **Nginx** দিয়ে সার্ভ করা হয়।

**কেন এটি দরকার? (Why do we need this?)**
- **User Interface**: প্রোডাক্ট ব্রাউজিং, কার্ট ম্যানেজমেন্ট এবং চেকআউট প্রসেস হ্যান্ডেল করে।
- **API Integration**: ব্যাকএন্ডের (`/api`) সাথে যোগাযোগ করে ডেটা আদান-প্রদান করে।
- **Performance**: Nginx এর মাধ্যমে স্ট্যাটিক অ্যাসেট দ্রুত ডেলিভারি দেয়।

---

## ⚙️ Technical Stack

| Component | Technology | Version | Purpose |
|-----------|------------|---------|---------|
| **Core** | React.js | ^18.2.0 | UI Library |
| **Tooling** | Create React App | 5.0.1 | Build Tool |
| **Styling** | Vanilla CSS | - | Custom Styles (`App.css`) |
| **Server** | Nginx | Alpine | Production Server |
| **Container** | Docker | - | Containerization |

---

## 📂 ফাইল স্ট্রাকচার (File Structure)

```bash
frontend/
├── src/
│   ├── components/     # রিইউজেবল কম্পোনেন্ট (Navbar, ProductCard)
│   ├── App.js          # মেইন অ্যাপ্লিকেশন লজিক
│   └── App.css         # গ্লোবাল স্টাইলিং
├── public/             # স্ট্যাটিক অ্যাসেটস (index.html, images)
├── build-production.sh # 🛠️ প্রোডাকশন বিল্ড স্ক্রিপ্ট
├── Dockerfile          # 🐳 ডকার ইমেজ কনফিগারেশন
└── nginx.conf          # 🌐 Nginx সার্ভার কনফিগারেশন
```

---

## 🚀 কিভাবে রান করবেন? (How to Run)

### ১. লোকাল ডেভেলপমেন্ট (Local Development)
সরাসরি আপনার কম্পিউটারে রান করার জন্য:

```bash
cd frontend
npm install
npm start
```
*Browser: http://localhost:3000*

### ২. প্রোডাকশন বিল্ড (Production Build)
লোকালি প্রোডাকশন ভার্সন টেস্ট করার জন্য:

```bash
# বিল্ড তৈরি করুন
npm run build

# অথবা আমাদের স্ক্রিপ্ট ব্যবহার করুন (Docker Build সহ)
./build-production.sh
```

### ৩. ডকার দিয়ে রান করা (Docker Run)
```bash
docker build -t dhakacart-frontend .
docker run -p 30080:80 dhakacart-frontend
```
*Browser: http://localhost:30080*

---

## 🔗 API Connection
এই ফ্রন্টএন্ডটি অটোমেটিক্যালি ব্যাকএন্ডের সাথে কানেক্ট করার জন্য কনফিগার করা আছে।
- **Local**: `package.json`-এ `proxy` সেট করা আছে (সাধারণত `http://localhost:5000`)।
- **Production**: Nginx `/api` রিকোয়েস্টগুলোকে ব্যাকএন্ড সার্ভিসে ফরওয়ার্ড করে (Reverse Proxy)।
