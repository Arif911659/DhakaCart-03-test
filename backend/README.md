# 🔙 DhakaCart Backend API

**এটি কি? (What is this?)**
এটি DhakaCart-এর "ব্রেইন" বা লজিক লেয়ার। এটি **Node.js (v18)** এবং **Express.js** দিয়ে তৈরি। এটি সম্পূর্ণ স্টেটলেস (Stateless) এবং কুবারনেটিসে স্কেল করার জন্য ডিজাইন করা।

**কেন এটি দরকার? (Why do we need this?)**
- **Business Logic**: অর্ডার প্রসেসিং, ইনভেন্টরি ম্যানেজমেন্ট।
- **Persistence**: PostgreSQL ডাটাবেসে ডাটা সেভ করা।
- **Caching**: Redis ব্যবহার করে হাই-স্পিড ডেটা রিট্রিভাল।
- **Security**: Helmet, Rate Limiting এবং Joi ভ্যালিডেশন ব্যবহার করা হয়েছে।

---

## ⚙️ Technical Stack

based on `package.json`:

| Component | Technology | Version | Purpose |
|-----------|------------|---------|---------|
| **Runtime** | Node.js | v18+ | JavaScript Runtime |
| **Framework**| Express.js | ^4.18.2 | REST API Framework |
| **Database** | PostgreSQL | ^8.11.3 (pg) | Primary Database |
| **Cache** | Redis | ^4.6.10 | Caching & Sessions |
| **Validation**| Joi | ^17.13.3 | Request Validation |
| **Security** | Helmet | ^7.2.0 | HTTP Header Security |

---

## 📂 ফাইল স্ট্রাকচার (File Structure)

```bash
backend/
├── server.js           # 🚀 মেইন অ্যাপ্লিকেশন এন্ট্রি পয়েন্ট
├── health-check.js     # 💓 হেলথ চেক স্ক্রিপ্ট (কন্টেইনার হেলথ প্রোব)
├── Dockerfile          # 🐳 প্রোডাকশন কন্টেইনার কনফিগারেশন
└── package.json        # 📦 ডিপেন্ডেন্সি এবং স্ক্রিপ্টস
```

> *নোট: `routes` বা `controllers` ফোল্ডার বর্তমানে `server.js` এর ভেতরেই হ্যান্ডেল করা হচ্ছে (Monolithic file structure for simplicity).*

---

## 🚀 কিভাবে রান করবেন? (How to Run)

### ১. লোকাল ডেভেলপমেন্ট (Local)
ডাটাবেস কানেকশন ছাড়া রান করলে এরর দিতে পারে।

```bash
cd backend
npm install

# ডেভেলপমেন্ট মোড (Nodemon সহ)
npm run dev

# অথবা প্রোডাকশন মোড
npm start
```
*API: http://localhost:5000*

### ২. ডকার দিয়ে (Docker)
PostgreSQL এবং Redis কানেকশন এনভাইরনমেন্ট ভেরিয়েবল হিসেবে দিতে হবে।

```bash
docker build -t dhakacart-backend .
docker run -p 5000:5000 \
  -e DB_HOST=host.docker.internal \
  -e REDIS_HOST=host.docker.internal \
  dhakacart-backend
```

---

## 🔌 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/api/health` | সিস্টেম হেলথ চেক |
| `GET` | `/api/products` | সব প্রোডাক্ট লিস্ট |
| `GET` | `/api/products/:id` | নির্দিষ্ট প্রোডাক্ট ডিটেইলস |
| `POST`| `/api/orders` | নতুন অর্ডার প্লেস করা |

---

## 🛡️ Security Features
- **Helmet**: HTTP হেডার সিকিউরিটি নিশ্চিত করে।
- **Rate Limiting**: DDoS আক্রমণ প্রতিরোধে রিকোয়েস্ট লিমিট করে।
- **CORS**: শুধুমাত্র ট্রাস্টেড ডোমেইন থেকে রিকোয়েস্ট এক্সেপ্ট করে।
